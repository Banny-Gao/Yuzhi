import { pages } from './generated.routes'

export default defineAppConfig({
  pages,
  window: {
    backgroundTextStyle: 'light',
    navigationBarBackgroundColor: '#fff',
    navigationBarTitleText: '豫知',
    navigationBarTextStyle: 'black',
  },
})
